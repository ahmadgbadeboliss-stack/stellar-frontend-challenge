# Screenshots

Place your submission screenshots here so the README renders them:

- `screenshot-connected.png` — wallet connected state
- `screenshot-balance.png` — balance displayed
- `screenshot-transaction.png` — successful testnet transaction with the result shown

Capture them from the running app (`npm run dev`) after connecting Freighter on Testnet.
